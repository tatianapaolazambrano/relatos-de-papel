import React from 'react';
import '../styles/Header.css';

const Header = () => {
    return (
        <header className="header">
            <p className="marca-libreria"> <i className="bi bi-book icon-book"></i> Librería Relatos de Papel </p>
        </header>
    );
};

export default Header;